<?php
/*Wenn ein Formular mit GET übermittelt wird, werden die eingegebenen Daten als Teil der URL an den Server gesendet.
Diese Daten können von jedem gesehen werden.
/*Wenn ein Formular mit POST übermittelt wird, werden die eingegebenen Daten im Body des HTTP-Requests an den Server gesendet 
und sind somit nicht in der URL sichtbar.
